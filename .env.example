# OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
# Required for embeddings (text-embedding-3-large) and chat completions (gpt-4o-mini)
OPENAI_API_KEY=sk-proj-your-api-key-here

# PostgreSQL Connection String with pgvector
# Format: postgresql+psycopg://username:password@host:port/database
# For local Docker setup (default):
POSTGRES_CONNECTION=postgresql+psycopg://langchain:langchain@localhost:5432/langchain

# For cloud PostgreSQL (example):
# POSTGRES_CONNECTION=postgresql+psycopg://user:password@your-host.example.com:5432/dbname
